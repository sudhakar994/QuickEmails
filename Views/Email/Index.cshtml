
@{
    ViewData["Title"] = "Index";
}

@*@using (Html.BeginForm("SaveGroupItem", "Faqs", FormMethod.Post, new { @Id = "frmAddGroup" }))
{

    <div class="modal-header">


        <h5 class="modal-title" id="modalHeader">
            @if (Model?.GroupItemId > 0)
            {
                <span>Edit  Group Item</span>
            }
            else
            {
                <span>Add Group Item</span>
            }

        </h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>


    </div>
    <div class="modal-body AddVideo">
        <div class="col-md-12 col-sm-12 col-xs-12 hidden-xs">

            <div>
                <div class="form-group">
                    <label for="articleurl"><span class="red">*</span>Group</label>
                    @Html.DropDownListFor(model => model.GroupId, dropDownGroup, "--select--", new { @class = "form-control" })
                </div>
                <div class="form-group">
                    <label for="ArticleName"> <span class="red">*</span>Group Item Name</label>
                    @Html.TextBoxFor(Model => Model.GroupItem, new { @class = "form-control", @onchange = "GenerateSlug()" })
                </div>

                @Html.HiddenFor(Model => Model.GroupItemId)
            </div>




        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-primary" onclick="fnSaveGroupItem()">Save Group</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
    </div>

}*@

